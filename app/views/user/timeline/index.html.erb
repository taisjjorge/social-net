<h2>Seja bem vindo ao painel do usuário</h2>

<div class="row">
  <div class="offset-2 col-lg-6" style="margin: 12px;">
    <% @posts.each do |post| %>
      <%= render "user/post_card", post: post %>
    <% end %>
  </div>
</div>